 /// @description Acciones

event_inherited();

if (minus_button.update || plus_button.update) {
	language_update(controller_game.language_id[option, 1]);
	with (class_label) {
		label_reload = true;
	}
}