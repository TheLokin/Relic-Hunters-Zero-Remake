/*
 *	input_keyboard_interaction()
 *
 *	Returns true if the button to interact with the keyboard is currently held down or false otherwise.
 *
 *	Returns: Boolean.
 */

return keyboard_check(global.keyboard_interaction);