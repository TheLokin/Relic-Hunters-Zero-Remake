/*
 *	input_keyboard_shot()
 *
 *	Returns true if the button to shot with the keyboard is currently held down or false otherwise.
 *
 *	Returns: boolean.
 */

return mouse_check_button(global.keyboard_shot);