/*
 *	input_keyboard_dash()
 *
 *	Returns true if the button to dash with the keyboard is currently held down or false otherwise.
 *
 *	Returns: boolean.
 */

return keyboard_check(global.keyboard_dash);