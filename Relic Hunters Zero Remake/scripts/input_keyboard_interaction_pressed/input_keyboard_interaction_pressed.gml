/*
 *	input_keyboard_interaction_pressed()
 *
 *	Returns true if the button to interact with the keyboard is pressed or false otherwise.
 *
 *	Returns: boolean.
 */

return keyboard_check_pressed(global.keyboard_interaction);