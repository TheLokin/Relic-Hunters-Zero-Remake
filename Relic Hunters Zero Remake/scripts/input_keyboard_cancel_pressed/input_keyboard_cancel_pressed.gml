/*
 *	input_keyboard_cancel_pressed()
 *
 *	Returns true if the button to cancel with the keyboard is pressed or false otherwise.
 *
 *	Returns: boolean.
 */

return keyboard_check_pressed(global.keyboard_cancel);