/// @description Actions

if (minus_button.update || plus_button.update) {
	minus_button.update = false;
	plus_button.update = false;
	if (is_array(label_option)) {
		label_text_second = label_option[option];
	}
}